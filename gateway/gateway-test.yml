server:
  port: 8081

eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
spring:
  cloud:
    gateway:
      discovery.locator:
        lowerCaseServiceId: true
        enabled: true
      routes:
        - id: diary-service
          uri: lb://BACKEND
          predicates:
            - Path=/diary/**
          filters:
            - RewritePath=/diary/(?<path>.*), /$\{path}
            - name: AddAuthInfoGatewayFilter
 
management:
  endpoints:
    web:
      exposure:
        include: "*"
